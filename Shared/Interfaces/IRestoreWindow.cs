namespace AOEOQuestEngine.CoreLibrary.Shared.Interfaces;
public interface IRestoreWindow
{
    //so if a window needs to be restored, then somebody needs to figure out how to restore the window.
    void RestoreWindow();
}